services:
  - type: web
    name: fastapi-loan-system
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    # startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
